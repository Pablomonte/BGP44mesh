# Netmaker Server Configuration
# Copy to .env and customize

# Server hostname (public domain or IP)
SERVER_HOST=netmaker.example.com
NETMAKER_API_PORT=8443
MQTT_PORT=1883
WIREGUARD_PORT=51821

# API Authentication (generate with: openssl rand -base64 32)
MASTER_KEY=changeme

# Network Configuration
MESH_NETWORK_ID=mesh
MESH_ADDRESS_RANGE=44.30.127.0/24
