# TINC configuration for {{ node_name }}
Name = {{ node_name }}
Mode = switch
Device = /dev/net/tun
Interface = tinc0
Port = {{ tinc_port }}

# Encryption
Cipher = aes-256-cbc
Digest = sha256
Compression = 11

# Connect to peers
{% for peer in tinc_peers %}
ConnectTo = {{ peer }}
{% endfor %}
